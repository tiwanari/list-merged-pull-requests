name: example

# on:
#   pull_request:
#     types: [opened]

jobs:
  example:
    name: release management comment
    runs-on: ubuntu-latest
    steps:
      - name: Comment on PR
        uses: tiwanari/list-merged-pull-requests@test
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
